# Local Supabase secrets (never commit real keys)
.env
.env.*

# Supabase CLI local state
supabase/.branches
supabase/.temp

# Vercel CLI local state
.vercel

# Dependencies (not used by this project, but never commit them)
node_modules

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
.env*
